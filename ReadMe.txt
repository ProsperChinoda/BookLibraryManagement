Welcome to the Book Library Management System

Cd Backend dir, run command pip install -r requirements.txt, create virtual env python -m venv .venv, and run backend using python manage.py runserver command
Cd Frontend, npm install axios react-router-dom jwt-decode, to start run command npm run dev

project still pending, and deployment
